[project]
name = "airdap-host-tools"
version = "0.1.0"
description = "Python environment for AirDAP host tools"
requires-python = ">=3.10"
dependencies = [
    "libusb-package>=1.0.30.0",
    "pyusb>=1.3.1",
]

[tool.uv]
package = false
